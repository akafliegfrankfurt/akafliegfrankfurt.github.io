{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Hugo Server Task",
            "type": "vsctask",
            "request": "launch",
            "task": "Debug: Start Hugo Server"
        }
    ]
}
